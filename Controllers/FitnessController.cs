namespace Fitness.Controllers
{
    // FitnessController služi kot posrednik med GUI in poslovno logiko
    public class FitnessController
    {
        // Metode za upravljanje z uporabniki, termini, treningi, itd.
        public bool Login(string username, string password)
        {
            // Implementacija logike za prijavo
            // Vrne 'true', če so podatki pravilni, sicer 'false'
            return true; // Ta vrstica je zgolj za demonstracijo
        }

        // Dodatne metode...
    }
}
